import React, { ReactElement } from "react";
import "./home.css";

const Home = (): ReactElement => {
  return (
    <div className="container">
      <h2>Welcome to Home</h2>
      <p>This is the Home component using TypeScript and CSS Modules.</p>
    </div>
  );
};

export default Home;
